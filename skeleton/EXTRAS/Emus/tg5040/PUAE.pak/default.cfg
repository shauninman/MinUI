bind Up = UP
bind Down = DOWN
bind Left = LEFT
bind Right = RIGHT
bind A / 2nd fire / Blue = A
bind B / Fire / Red = B
bind X / Yellow = X
bind Y / Green = Y
bind Start / Play = START
bind Select = SELECT
bind L / Rewind = L1
bind R / Forward = R1
bind L2 = L2
bind R2 = R2
bind L3 = L3
bind R3 = R3

